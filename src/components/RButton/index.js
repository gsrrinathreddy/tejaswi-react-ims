import * as React from 'react';
import Button from '@mui/material/Button';

export default function RButton() {
  return (
    <>
    <Button variant="outlined">Ordered</Button>
    <Button variant="outlined">Restocked</Button>
    </>
   
  );
}